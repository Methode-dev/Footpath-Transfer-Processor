# Footpath-Transfer-Processor
Tool that aims at generating footpath transfers from GTFS &amp; Geopackage OSM data for path solving purposes in public transit matters.

kdtree serves both spatial lookup and coverage filter

osmium tags-filter map.osm.pbf w/highway -o roads.osm
libxml2-dev
